// И ИЛИ

let isChecked = true,
    isSelected = false,
    isClosed = true;

console.log(isChecked && isClosed); // И
console.log(isChecked && isSelected);
console.log(isChecked || isSelected); // ИЛИ
